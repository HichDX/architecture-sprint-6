apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
  labels:
    app: scaletestapp
spec:
  type: LoadBalancer
  selector:
    app: scaletestapp
  ports:
    - port: 8080
      targetPort: 8080